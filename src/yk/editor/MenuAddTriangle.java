package yk.editor;

/**
 * Created with IntelliJ IDEA.
 * User: yuri
 * Date: 7/3/13
 * Time: 11:28 PM
 */
public class MenuAddTriangle extends MenuEntry {

    public MenuAddTriangle(String key, String name) {
        super(name);
    }

    @Override
    public void click(DDDEditor editor) {
//        Vec3f pos = editor.arrowSceneSpacePos;
//        editor.vertices.add(new Vertex(pos.add(0, 0, 0)));
//        editor.vertices.add(new Vertex(pos.add(0, 10, 0)));
//        editor.vertices.add(new Vertex(pos.add(0, 0, 10)));
//        editor.polygons.add(new Polygon(editor.vertices.size() - 3,editor.vertices.size() - 2, editor.vertices.size() - 1));
    }
}
